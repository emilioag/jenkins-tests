unclassified:
  globalLibraries:
    libraries:
      - name: "utils"
        defaultVersion: "master"
        retriever:
          modernSCM:
            scm:
              git:
                remote: $GLOBAL_LIBRARIES_REPOSITORY
                credentialsId: "githubpass"
