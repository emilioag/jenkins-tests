credentials:
  system:
    domainCredentials:
      - credentials:
          - usernamePassword:
              scope:    GLOBAL
              id:       githubpass
              username: emilioag
              password: ${GITHUB_PASS}
