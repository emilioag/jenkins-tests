credentials:
  system:
    domainCredentials:
      - credentials:
          - usernamePassword:
              scope:    GLOBAL
              id:       githubpass
              username: emilioag
              password: ${GITHUB_PASS}
          - certificate:
              scope: SYSTEM
              id:       kubernetes
              password: secret
              keyStoreSource:
                fileOnMaster:
                  keyStoreFile: /certs/minikube.pfx
